export const enum ROUTER_KEYS {
  ROOT = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  FORGET_PASSWORD = '/forgot-password',
  ALL_MATCH = '/*',
}

export const STORAGE_KEYS = Object.freeze({
  TOKEN: 'TOKEN',
})
